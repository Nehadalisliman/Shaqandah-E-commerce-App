name: shoqandafinview
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  firebase_core: ^3.10.1
  firebase_storage: 12.4.10
  flutter_screenutil: ^5.9.0
  # ✅ المكتبة دي هي اللي بتعالج الـ Caching والتحميل من URL الفايربيز
  cached_network_image: ^3.3.1
  flutter_rating_bar: ^4.0.1 # ضيفي السطر ده هنا
  responsive_framework: ^1.5.1 # تأكدي من أحدث إصدار
  flutter:
    sdk: flutter

  # تم حذف الـ equatable من تحت الـ flutter مباشرة لأنها تضاف كمكتبة منفصلة
  equatable: ^2.0.5
  pin_code_fields: ^8.0.1
  modal_progress_hud_nsn: ^0.5.1
  cloud_firestore: ^5.0.0
  go_router: ^13.2.0
  get_it: ^7.7.0
  firebase_auth: ^5.4.1
  flutter_bloc: ^8.1.6
  dartz: ^0.10.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

