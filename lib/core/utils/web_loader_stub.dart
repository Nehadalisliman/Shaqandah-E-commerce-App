import 'package:flutter/material.dart';

// نسخة الموبايل (الوهمية)
Object createWebImage(String src) => throw UnsupportedError('Not Web');

// دالة تسجيل الصور الوهمية للموبايل
void registerWebView(String viewId, String imageUrl) {
  // لا تفعل شيئاً في الموبايل
}